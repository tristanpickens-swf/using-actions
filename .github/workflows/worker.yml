name: Build and Test Worker
on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: docker build -t phonebook-worker .
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Test phase
        run: go test -v ./...
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Deploy the App
        run: docker run -d -p 8080:8080 phonebook-worker